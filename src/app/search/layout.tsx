export const metadata = {
    title: 'Search',
    description: 'Search for songs, artists, albums and more on Spotify.',
};

export default function SearchLayout({ children }: { children: React.ReactNode }) {
    return children;
}